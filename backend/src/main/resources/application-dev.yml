spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    show-sql: true

    hibernate:
      ddl-auto: update

logging:
  level:
    com.ardms: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG